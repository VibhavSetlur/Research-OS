# requirements.txt — mirrors pyproject.toml [project.dependencies] exactly.
# For development extras use: pip install -e ".[dev,web,literature]"
# This file exists only for Docker/CI compatibility; pyproject.toml is authoritative.

mcp>=1.0.0
pyyaml>=6.0
requests>=2.28
httpx>=0.25
pydantic>=2.0
pydantic-settings>=2.0
