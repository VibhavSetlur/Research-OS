I now have a complete picture. The wet_lab pack ships protocols + tools (8 protocols, 3 tools), but does not integrate with paper compile / dashboard / slides / poster / reviewer / drafter_loop. The literature gate is pack-agnostic (works generically). Let me produce the report.

| subsystem | verdict | evidence | remediation (if fail) |
|-----------|---------|----------|-----------------------|
| literature_gate | pass | `tools/actions/audit/step_literature.py` is pack-agnostic — it operates on the generic `step_summary.yaml.literature_required` flag and `workspace/<step>/literature/findings_vs_literature.md`; wet_lab steps participate by default and can opt out via `literature_required: false` for pure bench-prep steps. No pack-specific corpora/topics are declared, but none are required at the gate level. | — |
| paper_pdf | fail | `tools/actions/synthesis/typst.py` `VENUE_TEMPLATES` only ships `nature/science/nejm/cell/ieee_conf/neurips/acl/plos/generic_two_column/generic_thesis/humanities_essay/chicago_thesis`. No wet_lab-aware venue or section schema is registered, and `tool_paper_compile_typst` has no path that consumes the `methods_section_wet_lab` protocol's required granularity (RRID / lot / clone / passage tables). The protocol exists as guidance YAML only. | Register a wet_lab section-schema hook in `typst.py` (e.g. an `expected_sections` list — "Materials & Methods", "Reagents", "Instruments", "Ethics & Approvals", "Statistics") that pack registration can advertise. `tool_paper_compile_typst` should look up the active pack via `detect_active_pack` (extended to recognize wet_lab) and pass that section list to `drafter_loop.compute_metrics` so the M&M reagent / instrument / ethics blocks are graded as a coverage requirement, not optional. |
| dashboard_story | fail | `dashboard_v2.detect_active_pack` only returns `"qualitative"` or `"humanities"` (line 807-815) and the story renderer only invokes `render_qualitative_section` / `render_humanities_section` (line 1279-1294). Wet_lab artefacts — `workspace/reagents/`, `workspace/instruments/`, `sample_lineage.mermaid`, plate-map PNGs — never reach the story panel even though `tool_wet_lab_plate_map_render` and `tool_wet_lab_sample_lineage_export` write them. | Extend `detect_active_pack` to recognize wet_lab (config `pack: wet_lab` OR presence of `workspace/reagents/` / `workspace/instruments/`). Add a `dashboard_v2_wet_lab.py` sibling that renders the four-pillar story panel — SOP versions, reagent lot table, instrument run log table, sample lineage Mermaid embed — using the YAML files the wet_lab tools already write. Wire it into the `pack == "wet_lab"` branch alongside qualitative/humanities. |
| dashboard_explore | fail | Same root cause: dashboard_v2 has only one renderer entry point and it ignores wet_lab. The "explore" mode is a CSS/JS view-toggle over the same generated HTML (lines 270-380), so the missing wet_lab section is missing in both story and explore views. No filter chips, no per-step reagent / instrument breakdown, no plate-map carousel. | Use the same pack-renderer hook as dashboard_story. The wet_lab renderer should emit `explore-only` panels (reagent search/filter table, instrument-run sortable table, sample-lineage zoomable Mermaid) that the existing `<ro-mode-toggle>` will reveal when the user picks "Explore". No new tool needed — just the renderer. |
| slides | fail | `tools/actions/synthesis/slides.py` `SUPPORTED_TEMPLATES` ships 5 generic audiences (`conference_15min`, `lab_meeting_30min`, `defense_45min`, `conference_5min_lightning`, `public_outreach`). No wet_lab-tailored deck: no slide that surfaces plate-map figure, lineage diagram, or reagent table; no audience preset for "wet-lab lab meeting" where the methods slide expects clone+lot detail. The wet_lab pack does not register any new template. | Add a `wet_lab_lab_meeting.yaml` (and optionally `wet_lab_conference_15min.yaml`) under `assets/slide_templates/` whose `slides:` schema includes a `plate_map` slide (source: `tool_wet_lab_plate_map_render` output), a `reagents` slide (source: `workspace/reagents/*.yaml`), and an `instrument_setup` slide. Have the pack `register()` extend `SUPPORTED_TEMPLATES` at import time (or accept pack-supplied template dirs in `_load_template`). |
| poster | fail | `tools/actions/synthesis/poster_typst.py` `SUPPORTED_TEMPLATES` is fixed-list-only (`academic_36x48`, `academic_48x36`, `academic_a0_portrait`, `academic_a1_landscape`, `public_24x36`). All are STEM-generic. No wet_lab template surfaces the plate-map PNG or lineage Mermaid that the pack's tools produce, and no hero-figure selector knows to prefer plate-maps for a wet_lab project. | Add a `wet_lab_36x48.typ` template under `assets/poster_templates/` with explicit slots for: (a) plate-map figure, (b) sample-lineage tree, (c) reagent table micro-block. Either hard-register it in `SUPPORTED_TEMPLATES` or extend `poster_typst.py` to merge pack-contributed templates. The `_hero_figures` selector should also be taught that `plate_map.png` and `sample_lineage.png` are wet_lab hero candidates. |
| reviewer_scaffold | fail | `tools/actions/synthesis/reviewer.py` ships 7 personas under `assets/reviewer_personas/` (`domain_expert`, `methodology_skeptic`, `novelty_critic`, `presentation_critic`, `reproducibility_advocate`, `scope_creep_critic`, `statistician`) and the wet_lab pack contributes none. The generic `domain_expert` lens cannot ask about antibody validation, lot-to-lot variability, mycoplasma testing, or instrument calibration drift — the exact things wet-lab reviewers flag. | Ship `wet_lab_bench_reviewer.yaml` (red_flags: missing RRID, missing lot number, missing passage count, missing mycoplasma date, n=1 biological replicate, technical-vs-biological replicate confusion, missing IRB/IACUC/IBC ID) and `wet_lab_replication_reviewer.yaml`. Either bundle in `assets/reviewer_personas/` or extend `load_persona` to search pack-contributed persona dirs (preferred — keeps pack self-contained). Update `DEFAULT_PERSONAS` to include wet_lab personas when `detect_active_pack` returns `"wet_lab"`. |
| drafter_loop | fail | `drafter_loop.py` accepts `expected_sections: list[str]` (line 252, 552, 636) but nothing in the pack registration pipeline injects a wet_lab-specific list. The caller in `server.py` / `typst.py` passes the same generic sections regardless of pack, so the quality-score `section_coverage` metric does not penalize a wet-lab paper that is missing the `Reagents`, `Instruments`, or `Ethics` sections. The hook is *there*; the wiring is not. | In `PackRegistration` (or as a new field), let packs declare `expected_sections: dict[deliverable, list[str]]` (e.g. `paper: [Abstract, Introduction, Materials & Methods, Reagents, Instruments, Statistics, Ethics, Results, Discussion]`). `tool_paper_compile_typst` / `tool_slides_create` / `tool_poster_create` should look up `active_pack`'s declared sections and pass them as `expected_sections` to `draft_with_review_rewrite`. The wet_lab pack would declare its 9-section M&M schema in `__init__.py` alongside `tools=` and `protocols_dir=`. |

**Summary:** The wet_lab pack ships a strong protocol-and-tools layer (8 protocols across protocol/method/audit/output + 3 working tools for plate-map render, reagent stubbing, and lineage export) and its domain detector is sound. But it is structurally **disconnected from the entire downstream synthesis pipeline**: 6 of the 8 core subsystems fail because the pack-aware extension points in `dashboard_v2.detect_active_pack` (qualitative/humanities only), `typst.VENUE_TEMPLATES` (no wet_lab venue), `slides.SUPPORTED_TEMPLATES` (no wet_lab audience), `poster_typst.SUPPORTED_TEMPLATES` (no wet_lab layout), `reviewer.DEFAULT_PERSONAS` (no bench-reviewer persona), and `drafter_loop.expected_sections` (no per-pack section schema injection) were never built out for wet_lab even though qualitative and humanities packs received partial wiring. Only `literature_gate` works cleanly (because it is pack-agnostic by design) and pack registration / protocol loading via `PackRegistration` is fully functional. v2.0.0 closure for this pack is primarily a wiring-and-templates job, not a logic-design job — the pack already produces the right artefacts; the synthesis tools just need pack-aware dispatch hooks added.

Relevant file paths:
- /scratch/vsetlur/Research-OS/src/research_os_wet_lab/__init__.py
- /scratch/vsetlur/Research-OS/src/research_os_wet_lab/tools.py
- /scratch/vsetlur/Research-OS/src/research_os_wet_lab/router_entries.py
- /scratch/vsetlur/Research-OS/src/research_os_wet_lab/detector.py
- /scratch/vsetlur/Research-OS/src/research_os_wet_lab/protocols/{protocol,method,audit,output}/*.yaml
- /scratch/vsetlur/Research-OS/src/research_os/tools/actions/audit/step_literature.py
- /scratch/vsetlur/Research-OS/src/research_os/tools/actions/synthesis/typst.py
- /scratch/vsetlur/Research-OS/src/research_os/tools/actions/synthesis/slides.py
- /scratch/vsetlur/Research-OS/src/research_os/tools/actions/synthesis/poster_typst.py
- /scratch/vsetlur/Research-OS/src/research_os/tools/actions/synthesis/reviewer.py
- /scratch/vsetlur/Research-OS/src/research_os/tools/actions/synthesis/drafter_loop.py
- /scratch/vsetlur/Research-OS/src/research_os/tools/actions/synthesis/dashboard_v2.py
- /scratch/vsetlur/Research-OS/src/research_os/assets/reviewer_personas/
- /scratch/vsetlur/Research-OS/src/research_os/assets/slide_templates/
- /scratch/vsetlur/Research-OS/src/research_os/assets/poster_templates/