id: synthesis_poster
tier: 'synthesize'
name: Synthesis — Poster (redirect)
version: '2.0.0'
schema_version: '2.0'
scope_tags:
  domain: [any]
  audience: [auditor]
  workflow_shape: [any]
last_reviewed: '2026-06-04'
description: |
  Redirect stub. Poster content was consolidated into
  `synthesis/printable.yaml` parametrized by `format='poster'`.
  Loading this name continues to work (loader follows redirect_to);
  the deprecation is logged to `.os_state/deprecations.log` so the
  project can audit usage before the next major (when stubs are
  removed). Use `synthesis/printable` directly going forward.

redirect_to: synthesis/printable
redirect_params:
  format: poster
